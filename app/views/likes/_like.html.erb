<% if current_user.liked_by?(shop.id) %>
	<%= link_to 'Like', unlike_path(shop), method: :delete, remote: true %>  <%= shop.likes.count %>
<% else %>
	<%= link_to 'Unlike', like_path(shop), method: :post, remote: true %>  <%= shop.likes.count %>
<% end %>